# خارطةُ الطريق — GMD لأندرويد

ما بعدَ `26.9.0-beta.7` (‏2026-09-04). البنودُ مرتَّبةٌ بأثرِها على المستخدمِ لا
بسهولةِ تنفيذها، وكلُّ بندٍ يذكرُ ما يمنعُه إن كانَ ممنوعاً.

## أُنجِز في سلسلة beta

- **سجلُّ التنزيلات** — اقتراحُ المستخدم، بحذفٍ فرديٍّ ومنتقىً وجماعيٍّ وإعادةِ محاولة.
- **اقتصاصُ جزءٍ من المادّة** — بمسلكَين: طلبُ الجزءِ من الخادم، فإن رُفِضَ فالاقتصاصُ
  في الجهازِ بـffmpeg.
- **إعادةُ تفعيلِ R8** — الحزمةُ من 51.2 إلى 40.9 م.ب، وأسماءُ الاستثناءاتِ محفوظة.
  **يبقى التحقّقُ النهائيُّ بتشغيلِ الحزمةِ المصغَّرةِ على جهازٍ حقيقيّ.**
- **أسرارُ التوقيعِ ورمزُ النشر** — مضبوطةٌ كلُّها، فاكتملت سلسلةُ الإصدارِ في
  الآليّة: تبني وتوقّعُ وتتحقّقُ من بصمةِ الشهادةِ وتنشرُ باسمِ صاحبِ المستودع.
- **قوائمُ التشغيل** — تنزيلاً بالاختيار، ومجلَّداً لكلِّ قائمةٍ في المعرض، ومدخلاً
  واحداً في السجلّ.
- **تنزيلُ التحديثِ في الخلفيّة** مع استئنافٍ من موضعِ الانقطاع.
- **زرُّ الرجوعِ وتأكيدُ الخروج**، وعرضُ ملاحظاتِ الإصدارِ نصّاً مقروءاً.
- **المحدِّثُ الذاتيُّ يقرأُ لاحقةَ ما قبلَ الإصدار** — وقد تُحُقِّقَ منه على جهازٍ
  حقيقيّ: beta.2 رأت beta.3 من تلقائِها.
- **اقتصاصُ ملفٍّ موجودٍ في الجهاز** — شاشةٌ مستقلّةٌ وزرُّ مقصٍّ في المعرض.
- **قسمانِ مستقلّانِ يعملانِ معاً** — رابطٌ وقائمةٌ وتقدُّمٌ لكلِّ قسم، وتنزيلانِ
  متوازيان، واستعادةُ ما كانَ يجري بعدَ إغلاقِ التطبيقِ وفتحِه.
- **وسمُ المرحلةِ** (تنزيل · تحويل · تجميع · حفظ) وموضعُ العنصرِ من القائمة.
- **تبويبا المعرض** — مرئيّاتٌ وصوتيّات، وفي كلٍّ قوائمُ تشغيلِه ومقاطعُه المفردة،
  وترتيبُ القائمةِ محفوظٌ داخلَها.
- **السجلُّ يُكتَبُ عندَ بدءِ المحاولةِ** موسوماً «سارية» ثمّ يُختَمُ بنتيجتِه، وقسمٌ
  يعملُ لا يُكتَبُ فوقَه، ورابطٌ سبقَ تنزيلُه يُنبَّهُ عليه.
- **ما اكتملَ من القائمةِ يُنقَلُ إلى المعرضِ أوّلاً بأوّل** لا بعدَ آخرِ مقطعٍ فيها.
- **مشغّلٌ صوتيٌّ داخليّ** بشريطٍ دائمٍ واستئنافٍ من موضعِ الاستماعِ وتشغيلٍ للقائمةِ
  كلِّها، يعملُ خارجَ التطبيقِ بخدمةٍ مقدِّمة.

---

## أوّلاً: ما يمنعُ الاستقرار

### 1. التحقّقُ من الحزمةِ المصغَّرةِ على جهاز — **موقوفٌ على تجربةٍ حقيقيّة**

أُعيدَ تفعيلُ R8 في beta.1، وتُحُقِّقَ من داخلِ الحزمةِ أنّ أصنافَ المكتبةِ كلَّها
بقيت بأسمائِها ومنها ما يبنيه Jackson بالانعكاس، وأنّ التشويشَ عملَ في سائرِ
الشيفرة. لكنّ هذا يُثبِتُ أنّ الأصنافَ محفوظةٌ لا أنّ التطبيقَ يعمل: تهيئةُ الأدواتِ
لا يُثبِتُ نجاحَها إلّا تشغيلُ الحزمةِ على جهاز.

**العمل:** تُجرَّبُ beta.1 على جهازٍ حقيقيّ. فإن فشلت التهيئةُ وصلَ اسمُ الاستثناءِ
مقروءاً هذه المرّةَ لا مشوَّشاً، فيُضافُ ما ينقصُ من قواعدِ الحفظ.

### 2. اقتصاصُ ملفٍّ موجودٍ في الجهاز — **أُنجِز في beta.5**

شاشةٌ ثالثةٌ تختارُ من مقاطعِ GMD أو من أيِّ ملفٍّ في الجهازِ بنافذةِ وثائقِ النظام،
ومن المعرضِ زرُّ مقصٍّ لمقطعٍ واحد. والقصُّ بنسخِ التيّاراتِ في خدمةٍ مقدِّمة،
والأصلُ لا يُمَسّ.

**وما بقيَ فيه:** القطعُ يقعُ عندَ أقربِ إطارٍ مفتاحيّ. وقصٌّ دقيقُ الحدِّ يلزمُه
إعادةُ ترميز — دقائقُ من المعالجةِ ونصيبٌ من البطّاريّةِ لأجلِ جزءٍ من الثانية —
فيُترَكُ خياراً موسوماً بثمنِه إن طُلِب. ولا يُعرَفُ بعدُ ما يشحنُه ffmpeg المرافقُ
من مُرمِّزات، فيُتحقَّقُ منه أوّلاً.

---

### 3. ما يستحقُّ التجربةَ في المشغّل — **جديدٌ في beta.7**

المشغّلُ مبنيٌّ على `MediaPlayer` الإطاريِّ بلا مكتبةٍ خارجيّة، وثلاثةُ أمورٍ فيه لا
يُثبِتُها إلّا جهازٌ حقيقيّ: **الاستمرارُ بعدَ إغلاقِ التطبيق** (الخدمةُ مقدِّمةٌ
بإشعارٍ وقفلِ يقظة)، و**التنحّي لمكالمةٍ ثمّ العودة** (التركيزُ الصوتيّ)،
و**الاستئنافُ من الموضعِ المحفوظ** في مادّةٍ طويلة.

وما يُؤجَّلُ عن قصد: أزرارُ الشاشةِ المقفلةِ بأسلوبِ الوسائط (`MediaStyle`) تلزمُها
`androidx.media`، وليست في اعتماديّاتِ المشروعِ ولا في مخبأِ البناءِ المحلّيّ — تُضافُ
حينَ يكونُ اتّصالٌ ويُعادُ البناء.

---

## ثانياً: ما بقيَ من فجوةِ المزايا

| الميزة | الحاسوب | الهاتف |
|---|---|---|
| تنزيلُ وتحويلٌ مباشر | ✅ | ✗ |
| تحويلُ ملفٍّ موجود | ✅ | ✗ |
| خيارات إضافيّة (صورة مصغَّرة · ترجمات · وصف) | ✅ | ✗ |

وثنائيّا ffmpeg وffprobe مشحونانِ في الحزمةِ ومعروفا الموضعِ الآن، فالتحويلُ
والاقتصاصُ المحلّيّانِ لا يمنعُهما إلّا واجهةٌ لاختيارِ الملفّ.

## ثالثاً: سجلُّ التنزيلات — **أُنجِز**

اليومَ لا يبقى من محاولةٍ فاشلةٍ أثر: تُغلَقُ البطاقةُ الحمراءُ فيضيعُ الرابطُ
وسببُ الفشلِ معاً. ولا يعرفُ المستخدمُ ما جرّبَه قبلَ أسبوع، ولا يستطيعُ إعادةَ
محاولةِ رابطٍ فشلَ حينَ كانَ الاتّصالُ ضعيفاً.

**الشكل:** شاشةٌ ثالثةٌ إلى جانبِ المعرض، فالمعرضُ يعرضُ **الملفّاتِ** والسجلُّ
يعرضُ **المحاولات** — والفرقُ جوهريّ: محاولةٌ فاشلةٌ لا ملفَّ لها، وملفٌّ حذفَه
المستخدمُ تبقى محاولتُه.

**لكلِّ مدخل:** الرابطُ · العنوانُ والقناةُ والمدّةُ والصورةُ المصغَّرةُ إن جُلِبت ·
النوعُ (فيديو/صوت) والجودةُ أو الصيغةُ المطلوبة · **نجحَ أم فشل** · سببُ الفشلِ
كاملاً إن فشل · وقتُ المحاولةِ · وعنوانُ الملفِّ الناتجِ إن نجح.

**الأفعال:**
- نقرةٌ على مدخلٍ ناجح → يُشغَّلُ ملفُّه، فإن كانَ محذوفاً بُيِّنَ ذلك ولم يُترَك
  المستخدمُ أمامَ زرٍّ لا يفعلُ شيئاً.
- نقرةٌ على مدخلٍ فاشل → يُملأُ الرابطُ في شاشةِ التنزيلِ بجودتِه نفسِها لإعادةِ
  المحاولة. وهذا وحدَه يكفي مبرِّراً للميزة.
- نسخُ الرابط، ونسخُ نصِّ الخطأ.
- حذفٌ **فرديٌّ ومنتقىً وجماعيّ** بالنمطِ نفسِه الذي في المعرض: لمسةٌ مطوّلةٌ
  تُدخِلُ وضعَ التحديد، وزرٌّ لتحديدِ الكلّ. وحذفُ مدخلٍ لا يمسُّ الملفَّ المنزَّل.
- مُرشِّحٌ: الكلّ / الناجح / الفاشل.

**التخزين:** ملفُّ JSON في `filesDir` بـ`org.json` المضمَّنِ في أندرويد — لا Room
ولا `kotlinx.serialization`، فالمشروعُ لا يحملُ أيّاً منهما وإدخالُ محرِّكِ قاعدةِ
بياناتٍ لأجلِ قائمةٍ مسطَّحةٍ ثمنٌ بلا مقابل. ويُقتَصَرُ على آخرِ 500 مدخلٍ لئلّا
ينمو بلا حدّ.

**مزلقٌ يجبُ الانتباهُ له:** الرابطُ قد يحملُ رمزَ جلسةٍ أو معرّفاً خاصّاً، فالسجلُّ
خاصٌّ بالتطبيقِ ولا يُشارَكُ ولا يُرفَعُ ولا يدخلُ نسخةً احتياطيّة.

---

## ثالثاً: ما تملكُه نسخةُ الحاسوبِ ولا تملكُه نسخةُ الهاتف

| الميزة | الحاسوب | الهاتف |
|---|---|---|
| تنزيل فيديو وصوت | ✅ | ✅ |
| تنزيل وتحويل مباشر | ✅ | ✗ |
| تحويل ملفٍّ موجود | ✅ | ✗ |
| قصُّ فيديو/صوت | ✅ | ✅ (اقتصاصٌ عند التنزيل) |
| خيارات إضافيّة (صورة مصغَّرة · ترجمات · وصف) | ✅ | ✗ |
| معلومات الوسائط | ✅ | ✅ |
| معرض داخل التطبيق | ✗ | ✅ |
| مشاركة الرابط إلى البرنامج | ✗ | ✅ |

وأُنجِزَ منها الاقتصاصُ في beta.1، غير أنّه يقتصُّ عندَ التنزيلِ ولا يقتصُّ ملفّاً
موجوداً في الجهاز — وذلك بندٌ باقٍ: **اقتصاصُ ملفٍّ من المعرض**، وثنائيُّ ffmpeg
صارَ معروفَ الموضعِ فلا يمنعُه شيءٌ إلّا واجهةٌ لاختيارِ الملفّ.

---

## رابعاً: صيانةٌ مؤجَّلة

- **مجموعُ تحقُّقٍ داخلَ التطبيق**: المحدِّثُ الذاتيُّ ينزّلُ الحزمةَ ولا يقارنُ
  بصمتَها بـ`SHA256SUMS.txt` المنشورِ معها.
