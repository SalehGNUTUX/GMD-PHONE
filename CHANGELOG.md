# سجلُّ التغييرات

كلُّ تغييرٍ ملحوظٍ في GMD لأندرويد.
الصيغةُ مبنيّةٌ على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)،
والترقيمُ سلسلةٌ سنةٌ‑شهر تُكتَبُ semver بلا صفرٍ بادئ — سلسلةُ 26.09 رقمُها
`26.9.0` — وهي السلسلةُ نفسُها التي تحملُها نسخةُ الحاسوب.

---

## [26.9.0-beta.9] — 2026-09-04

### أُصلح

- **شريطُ المشغّلِ كانَ محجوباً تحتَ أزرارِ النظام.** فـ`Scaffold` يُزيحُ شريطَه
  العلويَّ عن شريطِ الحالةِ من تلقائِه ولا يفعلُ ذلك في السفليّ — فيقعُ نصفُ
  المشغّلِ خلفَ شريطِ التنقُّل. صارت الإزاحةُ على محتواه لا على سطحِه، فيبقى لونُ
  الشريطِ ممتدّاً خلفَ أزرارِ النظامِ بدلَ أن ينقطعَ دونَها.

---

## [26.9.0-beta.8] — 2026-09-04

### أُصلح

- **تبويبُ الصوتيّاتِ كانَ يبتلعُ قوائمَ التشغيل.** فرعُ «لا شيءَ هنا» يقيسُ المقاطعَ
  المفردةَ وحدَها، وتبويبُ الصوتيّاتِ قد لا يكونُ فيه مقطعٌ مفردٌ وفيه قائمتان —
  فيُقالُ لصاحبِها لا شيءَ هنا والعدّادُ فوقَه يقولُ «قائمتان». صارَ القياسُ
  بالمقاطعِ **والقوائمِ** معاً. (عطبٌ استُحدِثَ في beta.7 مع التبويبَين.)
- **وزرُّ التشغيلِ كانَ يُسلّمُ الصوتَ لمشغّلِ النظام** — في السجلِّ وفي تنبيهِ «سبقَ
  تنزيلُه» — لا للمشغّلِ الداخليِّ الذي بُنيَ لذلك. وأسوأُ منه أنّ مدخلَ قائمةِ
  التشغيلِ واحدٌ بمجلَّدِها لا سطرٌ لكلِّ ملفّ، فما كانَ يُشغَّلُ إلّا آخرُ مقطعٍ
  حُفِظ. صارَ الصفُّ يُبنى من مجلَّدِ القائمةِ في المعرضِ مرتَّباً بالاسمِ — والاسمُ
  يبدأُ برقمِ العنصر — فتُسمَعُ كلُّها كما طلبَ صاحبُها.

### أُضيف

- **بطاقةُ متجرٍ بالعربيّةِ والإنجليزيّة** في `fastlane/metadata/android/`: العنوانُ
  والوصفانِ وسجلُّ الإصدار، وفيها المطوّرُ والرخصةُ صراحةً — وهي ما يقرؤه F-Droid
  وما يشبهُه. ومعها الرخصةُ والمطوّرُ في بيانات الحزمةِ نفسِها، فمثبِّتُ أندرويد لا
  حقلَ فيه لهما لكنّ أدواتِ الفحصِ تقرؤُها.

---

## [26.9.0-beta.7] — 2026-09-04

### أُضيف

- **مشغّلٌ صوتيٌّ داخليّ.** يُشغّلُ المقطعَ المفردَ أو القائمةَ كلَّها بترتيبِها،
  و**يُكمِلُ من موضعِك**: موضعُ كلِّ مقطعٍ محفوظٌ باسمِ عنوانِه، فمن تركَ كتاباً
  صوتيّاً في دقيقتِه الأربعينَ يعودُ إليها لا إلى أوّلِه. ويُستعادُ آخرُ صفٍّ
  استُمِعَ إليه عندَ فتحِ التطبيقِ موقوفاً عندَ موضعِه، فنقرةٌ واحدةٌ تُكمِلُ ما بدأ.
- **وشريطُه يبقى ظاهراً في كلِّ شاشة** — بعنوانِ المقطعِ وموضعِه وزرَّي التشغيلِ
  والتالي — فالاستماعُ لا يُلغي التصفُّحَ ولا التنزيل، ونقرةٌ عليه تفتحُ المشغّلَ
  كاملاً بمقبضِ الزمنِ وصفِّ التشغيل.
- **والاستماعُ يُكمِلُ خارجَ التطبيق**: خدمةٌ مقدِّمةٌ بإشعارٍ فيه تشغيلٌ وسابقٌ
  وتالٍ وإغلاق، وقفلُ يقظةٍ فلا يقطعُه نومُ المعالج. ويتنحّى المشغّلُ لمكالمةٍ أو
  لتطبيقٍ آخرَ ثمّ يعودُ — بالتركيزِ الصوتيِّ لا بالمزاحمة.
- والمشغّلُ من إطارِ أندرويد نفسِه لا من مكتبةِ تشغيلٍ خارجيّة: المادّةُ ملفّاتٌ
  محلّيّةٌ لا بثٌّ متكيّف، وإدخالُ مكتبةٍ كاملةٍ لأجلِ ذلك ثمنٌ في حجمِ الحزمةِ بلا
  مقابل.

### تغيَّر

- **المعرضُ صارَ تبويبَين في الأعلى** — المرئيّاتُ والصوتيّات — وفي كلِّ تبويبٍ
  قسمانِ بعنوانَيهما وعددِهما: قوائمُ التشغيلِ ثمّ المقاطعُ المفردة. وكانت
  الصوتيّاتُ قسماً أسفلَ كلِّ المرئيّاتِ لا تُبلَغُ إلّا بتمريرٍ طويل.
- **وترتيبُ قائمةِ التشغيلِ يُحتَرَمُ داخلَها.** كانَ الفرزُ بتاريخِ الإضافةِ فيخلطُها
  — السادسُ قبلَ الأوّلِ لأنّ التنزيلَ لا يمضي على ترتيبِ القائمةِ دائماً — وصارَ
  بالاسمِ، وهو يبدأُ برقمِ العنصر.

---

## [26.9.0-beta.6] — 2026-09-04

كلُّها من تجربةِ beta.5 على جهازٍ حقيقيّ.

### أُصلح

- **المحاولةُ تدخلُ السجلَّ عندَ بدئِها لا عندَ نهايتِها.** كانَ المستخدمُ يفتحُ
  السجلَّ وتنزيلُ قائمةٍ يجري فلا يجدُ له أثراً — وقد يطولُ ساعةً. صارَ المدخلُ
  يُكتَبُ موسوماً **«سارية»** لحظةَ البدءِ ثمّ يُختَمُ بنتيجتِه **في موضعِه نفسِه**
  لا في مدخلٍ ثانٍ. وما بقيَ «سارياً» من جلسةٍ ماتت — خدمةٌ قُتِلت مع عمليّتِها فلم
  يُكتَب ختامُها — يُختَمُ ملغًى عندَ الإقلاعِ بدلَ أن يبقى يكذبُ على صاحبِه.
- **وقسمٌ يعملُ لا يُكتَبُ فوقَه.** كانت نقرةٌ على بطاقةٍ في السجلِّ تضعُ رابطَها في
  قسمِها بلا سؤال، فيحلُّ محلَّ رابطٍ يُنزَّلُ الآنَ فيغيبُ عن صاحبِه تقدُّمُه وما
  تمَّ من قائمتِه — والتنزيلُ يُكمِلُ في الخلفيّةِ لا يدري به. صارَ يُسألُ أوّلاً
  ويُعرَضُ عليه فتحُ القسمِ ليرى ما يجري. **وحقولُ القسمِ تُقفَلُ ما دامَ يعمل** —
  الرابطُ والجودةُ والصيغةُ والاقتصاص — فلا يقعُ الطمسُ من بابٍ آخر.
- **وما اكتملَ من القائمةِ يُنقَلُ إلى المعرضِ أوّلاً بأوّل.** كانت مقاطعُها تبقى في
  مجلَّدِ العملِ حتّى ينتهيَ آخرُها ثمّ تُنقَلُ دفعةً واحدة، فيفتحُ صاحبُها المعرضَ
  وقد نزلَ خمسةٌ من سبعةٍ فلا يجدُ شيئاً. والتمييزُ بترتيبِ العنصرِ في اسمِ الملفِّ
  لا بحداثتِه: فـyt-dlp يكتبُ أثناءَ العنصرِ الجاري قِطَعاً تبدو مكتملةً (`.f137.mp4`
  قبلَ التجميع)، ونقلُها يُفسِدُ المقطعَ ويُهدِرُ ما نزل.
- **ورابطٌ سبقَ تنزيلُه ونجح** يُنبَّهُ عليه صاحبُه قبلَ أن يُعادَ — ومعه موضعُ
  الملفِّ وزرُّ تشغيلِه — بدلَ أن تُنفَقَ بياناتٌ وزمنٌ ومساحةٌ على ما في اليد.
  والمطابقةُ على مفتاحٍ مطبَّعٍ لا على النصِّ الخام: روابطُ المشاركةِ تحملُ `si=`
  مختلفاً في كلِّ مرّةٍ فلا يتطابقُ حرفانِ لمقطعٍ واحد.

---

## [26.9.0-beta.5] — 2026-09-04

### أُضيف

- **اقتصاصُ ملفٍّ موجودٍ في الجهاز.** كانَ الاقتصاصُ يقعُ عندَ التنزيلِ وحدَه، فمن
  نزّلَ مقطعاً كاملاً بالأمسِ لزمَه أن يُعيدَ تنزيلَه ليقتصَّ منه اليوم. وشاشةٌ ثالثةٌ
  تختارُ من مقاطعِ GMD أو من أيِّ ملفٍّ في الجهازِ بنافذةِ وثائقِ النظام، ومن المعرضِ
  زرُّ مقصٍّ يعملُ حينَ يكونُ المحدَّدُ واحداً. والأصلُ لا يُمَسّ: الناتجُ ملفٌّ جديدٌ
  إلى جانبِه. وffmpeg عمليّةٌ أصليّةٌ لا تفهمُ `content://`، فيُقرَأُ المسارُ الحقيقيُّ
  إن كانَ مقروءاً وإلّا نُسِخَ الملفُّ أوّلاً بتقدُّمٍ ظاهر.
- **وسمُ المرحلةِ مع النسبة.** كانَ الشريطُ يقفُ عندَ 100٪ بلا كلمةٍ حينَ ينتهي
  التنزيلُ ويبدأُ ما بعدَه — استخراجُ الصوتِ أو تجميعُ التيّارَين أو النقلُ إلى
  المعرض — فيظنُّ المستخدمُ أنّ البرنامجَ تجمّدَ وقد يُلغي عملاً كادَ يتمّ. صارَ
  المكتوبُ: «جارٍ التنزيل» ثمّ «جارٍ التحويل» أو «جارٍ تجميع الصوت والصورة» ثمّ
  «جارٍ الحفظ في المعرض»، والشريطُ غيرُ محدَّدٍ فيما لا نسبةَ له.
- **وموضعُ العنصرِ من قائمةِ التشغيل**: «العنصر 3 من 5»، ومع كلِّ عنصرٍ في القائمةِ
  علامتُه — ما تمَّ بعلامةِ صحٍّ وما يجري بدوّارة. فكانَ المستخدمُ يرى نسبةً واحدةً
  لا تدلُّ على أيِّ عنصرٍ هي.
- **قسمانِ في المعرض**: المرئيّاتُ والصوتيّات، وفي كلٍّ منهما قوائمُ تشغيلِه
  وملفّاتُه المفردة. وكانَ الكلُّ مُلقًى في قائمةٍ واحدةٍ فيضيعُ مقطعٌ صوتيٌّ بينَ
  عشراتِ المرئيّات. والمجلَّداتُ تُجمَّعُ على الاسمِ **والنوعِ** معاً، فـ`Movies/GMD/رحلة`
  و`Music/GMD/رحلة` مجلَّدانِ مختلفانِ يتشابهُ اسماهما.

### تغيَّر

- **القسمانِ صارا مستقلَّينِ يعملانِ معاً.** كانَ الرابطُ والقائمةُ والتقدُّمُ حقلاً
  واحداً يتقاسمُه القسمان، فيرى المستخدمُ في «تنزيل فيديو» رابطَ الصوتِ وقائمتَه
  وتقدُّمَه، ولا يستطيعُ أن يُنزّلَ في أحدِهما وهو يعملُ في الآخر. صارَ لكلِّ قسمٍ
  حالتُه كاملةً — رابطُه ومعلوماتُه وقائمتُه وجودتُه وحدّا اقتصاصِه — و**تنزيلانِ
  يجريانِ معاً** في مهمّتَين مستقلّتَين، لكلٍّ عمليّتُها ومجلَّدُ عملِها وإلغاؤها.
  وشاشةُ «معلومات الوسائط» كذلك برابطِها الخاصّ، فسؤالٌ عن مقطعٍ لا يُبدِّلُ تنزيلاً
  جارياً.
- **وما كانَ يعملُ حينَ أُغلِقَ التطبيقُ يعودُ إلى شاشتِه**: الخدمةُ تبقى حيّةً
  بإشعارِها بينما تُهدَمُ الشاشةُ ونموذجُها معها، فكانَ المستخدمُ يعودُ إلى حقلٍ
  فارغٍ وشريطٍ ساكنٍ والتنزيلُ يجري في الخلفيّة. صارت الروابطُ والخياراتُ والتقدُّمُ
  تعودُ إلى موضعِها، وكلُّ قسمٍ إلى قسمِه.
- **وإشعارُ العملِ الجاري يذكرُ القسمَ والمرحلةَ**، ولكلِّ قسمٍ إشعارُ نهايةٍ مستقلٌّ
  فلا يمحو انتهاءُ أحدِهما خبرَ الآخر. وزرُّ الإيقافِ يوقفُ المهمّةَ المعنيّةَ حينَ
  تكونُ واحدةً وكلَّ ما يجري حينَ تكونانِ اثنتَين.

### أُصلح

- **مجلَّدُ العملِ كانَ واحداً يُمسَحُ في مطلعِ كلِّ تنزيل**، فلو جرى تنزيلان معاً محا
  أحدُهما ما نزّلَه الآخرُ لتوِّه. صارَ لكلِّ نوعٍ مجلَّدُه، وللاقتصاصِ مجلَّدُه.
- **وخطأُ آخرِ محاولةٍ كانَ حقلاً مشترَكاً** في كائنٍ مفرد، فيُنسَبُ خطأُ مهمّةٍ إلى
  أختِها. صارَ صندوقاً لكلِّ مهمّة.
- **واستدعاءُ ffmpeg صارَ مشترَكاً** بينَ الاقتصاصِ عندَ التنزيلِ والاقتصاصِ في
  الجهاز، فلا تُصلَحُ علّةٌ في أحدِهما وتبقى في الآخر.

---

## [26.9.0-beta.4] — 2026-09-03

### أُضيف

- **تنزيلُ قوائمِ التشغيل** بأسلوبِ نسخةِ الحاسوبِ وقالبِ الهاتف. يُكشَفُ الرابطُ
  تلقائيّاً بـ`--flat-playlist` — طلبٌ واحدٌ مهما طالت القائمة — وتُعرَضُ عناصرُها
  بمربّعاتِ اختيارٍ وزرِّ تحديدِ الكلّ، والعدَدُ المختارُ ظاهرٌ على زرِّ التنزيل.
  وهي بطاقةٌ في الشاشةِ لا نافذةٌ منبثقةٌ كما في الحاسوب: الشاشةُ ضيّقةٌ والنافذةُ
  تحجبُ الجودةَ والصيغةَ اللتَين قد يريدُ المستخدمُ تغييرَهما قبلَ التنزيل.

  **ولكلِّ قائمةٍ مجلَّدٌ خاصٌّ بها** — `Movies/GMD/‹اسم القائمة›` — ويُكتَبُ ترتيبُ
  العنصرِ في اسمِ الملفِّ فيبقى ترتيبُ القائمةِ ظاهراً مهما رتّبَ العارضُ أبجديّاً.
  ويُمرَّرُ `--ignore-errors` فعنصرٌ محجوبٌ في قائمةٍ من ثلاثينَ لا يُهدِرُ التسعةَ
  والعشرين، ولذلك صارَ النجاحُ يُقاسُ بما نزلَ لا برمزِ الخروجِ وحدَه.

- **المعرضُ صارَ ذا مستوَيَين**: قوائمُ التشغيلِ بطاقاتِ مجلَّداتٍ في الجذرِ — بصورةِ
  أحدثِ عنصرٍ وعددِ المقاطعِ ومجموعِ الحجم — تُفتَحُ على عناصرِها، والملفّاتُ المفردةُ
  تحتَها. وزرُّ الرجوعِ يُغلقُ المجلَّدَ قبلَ أن يعودَ إلى القائمة. والتجميعُ يقعُ على
  `RELATIVE_PATH` في MediaStore لا على سجلٍّ منفصلٍ قد يتباعدُ عن الواقع.

- **والسجلُّ يذكرُ القائمةَ مدخلاً واحداً** بعنوانِها ومسارِها و«حُفِظَ 12 من 15»، لا
  سطراً لكلِّ ملفّ: المستخدمُ طلبَ قائمةً فيُعرَضُ له ما طلب.

### أُصلح

- **تنزيلُ التحديثِ صارَ في خدمةٍ مقدِّمة.** كان يجري في نطاقِ الـViewModel، وأندرويد
  يُجمّدُ العمليّةَ حينَ تغادرُ الواجهةُ المقدّمةَ — فحزمةٌ تقاربُ 40 م.ب تموتُ في
  منتصفِها كلّما نظرَ المستخدمُ في تطبيقٍ آخر. والاستئنافُ من موضعِ الانقطاعِ كان
  مبنيّاً سلفاً (ملفُّ `.part` وترويسةُ `Range`) ولم يكن ينقصُه إلّا أن يبقى حيّاً.
- **زرُّ الرجوعِ كان يخرجُ من التطبيقِ من أيِّ شاشةٍ بلا سؤال.** صارَ يعودُ إلى
  القائمةِ من الشاشاتِ الفرعيّة، ولا يخرجُ إلّا من القائمةِ وبعدَ تأكيدٍ يذكرُ أنّ
  التنزيلَ الجاريَ يُكمِلُ في الخلفيّة.
- **ملاحظاتُ الإصدارِ كانت تُعرَضُ ترميزاً خاماً** — النجومُ والشَّرَطاتُ حروفاً بينَ
  يدَي القارئ. صارَ العريضُ عريضاً والشيفرةُ بخطٍّ ثابتٍ والقوائمُ بنقاط، بمحوِّلٍ
  صغيرٍ يكفي ما يَرِدُ في سجلِّ هذا المشروعِ بدلَ مكتبةِ Markdown كاملةٍ لأجلِ بطاقة.

---

## [26.9.0-beta.3] — 2026-09-03

**لا تغييرَ في الميزاتِ ولا في السلوك.** هذا الإصدارُ يخدمُ غرضَين لا ثالثَ لهما،
وذُكِرَ ذلك صراحةً كي لا يبحثَ أحدٌ عن جديدٍ ليس فيه:

- **أوّلُ إصدارٍ تبنيه وتوقّعُه وتنشرُه الآليّةُ وحدَها** من وسمٍ مدفوع، بلا جهازِ
  صاحبِ المشروع. وكانت كلُّ إصداراتِ المشروعِ قبلَه تُبنى محلّيّاً وتُرفَعُ يدويّاً،
  لأنّ أسرارَ التوقيعِ ورمزَ النشرِ لم تكن مضبوطة.
- **ويُتيحُ لمن على beta.2 اختبارَ التحديثِ الذاتيِّ بعدَ إصلاحِه.** فمقارنةُ النسخِ
  في beta.1 وما قبلَها كانت تُهمِلُ لاحقةَ ما قبلِ الإصدار، وأوّلُ إثباتٍ لصحّةِ
  الإصلاحِ أن ترى beta.2 هذا الإصدارَ من تلقائِها.

### تغيَّر

- أسرارُ التوقيعِ الأربعةُ ورمزُ النشرِ صارت مضبوطةً في المستودع، فاكتملت سلسلةُ
  الإصدارِ في الآليّة: فحوصٌ، فبناءٌ موقَّع، فتحقُّقٌ من بصمةِ الشهادة، فمجاميعُ
  تحقُّق، فنشرٌ باسمِ صاحبِ المستودعِ لا باسمِ الآليّة.

---

## [26.9.0-beta.2] — 2026-09-03

عطبانِ ظهرا في تجربةِ beta.1 على جهازٍ حقيقيّ، وكلاهما في التحديثِ نفسِه.

### أُصلح

- **المحدِّثُ الذاتيُّ كان يُهمِلُ لاحقةَ ما قبلَ الإصدارِ كلَّها**، فيقرأُ
  `26.9.0-alpha.1` و`26.9.0-beta.1` رقماً واحداً `26.9.0` ويقول «أنت على أحدثِ
  إصدار» وأنت على أقدمِهما. والعطبُ أعمُّ ممّا يظهر: التطبيقُ لا ينتقلُ بينَ أيِّ
  إصدارَينِ تجريبيَّينِ يشتركانِ في الرقم، ولا من تجريبيٍّ إلى مستقرٍّ يحملُ رقمَه.

  صارت المقارنةُ بترتيبِ semver الصحيح: الرقمُ أوّلاً، ثمّ المستقرُّ يسبقُ ما قبلَه
  عندَ التساوي، ثمّ تُقارَنُ المعرّفاتُ واحداً واحداً — الرقميُّ يسبقُ النصّيَّ،
  والرقميُّ يُقارَنُ عدداً لا حرفاً فـ`beta.10` بعدَ `beta.9` لا قبلَها. وتُحقِّقَ
  من ثمانِ حالاتٍ حدّيّةٍ قبلَ الاعتماد.

  ولذلك **لا يستطيعُ من كان على alpha.1 أو beta.1 أن يترقّى تلقائيّاً**: العطبُ في
  النسخةِ المثبَّتةِ عندَه لا في المنشورة. تُثبَّتُ beta.2 يدويّاً مرّةً واحدةً ثمّ
  يعملُ التحديثُ الذاتيُّ بعدَها.

- **زرُّ تحديثِ yt-dlp كان صامتاً**: يُنقَرُ فلا دوّارةَ ولا رسالة، ولا يُدرى أعملَ
  أم لم يعمل — إذ كان الاستدعاءُ الراجعُ فارغاً ولا حالةَ تُعرَض. صارت له مراحلُ
  مكشوفةٌ كتحديثِ البرنامجِ نفسِه: دوّارةٌ أثناءَ العمل، ثمّ «على أحدثِ نسخةٍ أصلاً»
  أو «حُدِّثَ إلى ‹الرقم›» أو نصُّ الخطأ. ونصُّ الزرِّ كان «تحقّق من وجود تحديث» —
  مفتاحَ زرِّ تحديثِ البرنامجِ نفسِه — وهو يُحدِّثُ الأداةَ لا يتحقّقُ فقط.

---

## [26.9.0-beta.1] — 2026-09-03

أوّلُ إصدارٍ **beta**: خرجَ من طورِ «يُنشَر للاختبارِ لا للاستعمال» بعد أن نجحت تجربةُ
alpha.1 على جهازٍ حقيقيٍّ في التثبيتِ والتنزيلِ والتصفّحِ والحفظ.

### أُضيف

- **سجلُّ التنزيلات.** كلُّ رابطٍ جُرِّب يبقى — نجحَ أو فشل — بعنوانِه وقناتِه وصورتِه
  المصغَّرةِ ونوعِه وجودتِه ووقتِ المحاولةِ وسببِ الفشلِ كاملاً إن فشل. والفرقُ بينَه
  وبينَ المعرضِ جوهريّ: المعرضُ يعرضُ **الملفّات** والسجلُّ يعرضُ **المحاولات**،
  فمحاولةٌ فاشلةٌ لا ملفَّ لها وملفٌّ حذفَه المستخدمُ تبقى محاولتُه.

  وأنفعُ ما فيه إعادةُ المحاولة: نقرةٌ على مدخلٍ فاشلٍ تملأُ الرابطَ وتختارُ الجودةَ
  والمقطعَ الزمنيَّ نفسَه وتقفزُ إلى شاشتِه. وكانَ إغلاقُ البطاقةِ الحمراءِ قبلَ اليومِ
  يُضيعُ الرابطَ وسببَ الفشلِ معاً فلا يبقى ما تُعادُ به الكرّة.

  ومعه حذفٌ فرديٌّ ومنتقىً وجماعيّ، ومُرشِّحُ (الكلّ · الناجح · الفاشل)، ونسخُ الرابطِ
  ونسخُ نصِّ الخطأ. وحذفُ مدخلٍ لا يمسُّ الملفَّ المنزَّل.

- **اقتصاصُ جزءٍ من المادّة.** يُكتَبُ حدّانِ — `0:30` أو `1:05:20` أو ثوانٍ مجرّدة —
  فيُنزَّلُ الجزءُ وحدَه. والتنفيذُ مسلكانِ متتاليان: يُطلَبُ الجزءُ من الخادمِ أوّلاً
  فلا يُنزَّلُ ما لا يُراد — وهو على الهاتفِ توفيرٌ حقيقيٌّ في البياناتِ والوقت — فإن
  رفضَ الموقعُ ذلك نُزِّلت المادّةُ كاملةً واقتُصّت في الجهازِ بـffmpeg، بلا تدخُّلٍ من
  المستخدمِ ولا سؤال. ويوتيوب من الرافضين: يردُّ 403 على جلبِ ffmpeg نطاقاً من روابطِه،
  وقد تُحقِّقَ من ذلك بالتجربةِ لا بالافتراض.

### تغيَّر

- **أُعيدَ تفعيلُ التصغير** (‏R8) وتقليصُ الموارد، فصَغُرت الحزمةُ بنحوِ الثلث. وقد
  تبيَّنَ أنّ مكتبةَ youtubedl-android **لا تشحنُ قواعدَ حفظٍ إطلاقاً** — لا
  `proguard.txt` في حزمتِها — وأنّ قواعدَ المشروعِ كُتبت بعدَ انهيارِ alpha.1 ولم
  تُختبَر قطُّ مع تصغيرٍ مُفعَّل.
- **أسماءُ الاستثناءاتِ تبقى مقروءةً بعدَ التصغير** (`-keepnames class * extends
  java.lang.Throwable`). و`P2.f` الذي أسقطَ alpha.1 لم يكن رسالةَ خطأٍ بل اسمَ صنفِ
  استثناءٍ شوّشه R8؛ وبلا جهازٍ متّصلٍ بـlogcat فاسمُ الاستثناءِ كلُّ ما يصلُ من جهازِ
  المستخدم. فإن انهارَ شيءٌ بعدَ اليومِ وصلَ باسمِه صريحاً.

---

## [26.9.0-alpha.1] — 2026-09-02

أوّلُ إصدارٍ لنسخةِ الهاتف، **تجريبيٌّ أوّليّ**: يُنشَر للاختبارِ لا للاستعمالِ
اليوميّ. والحزمةُ مبنيّةٌ بلا تصغير، فالتجربةُ على جهازٍ حقيقيٍّ أثبتت أنّ تعطيلَه
يُنجحُ تهيئةَ الأدوات — أي أنّ R8 هو سببُ عطبِ التهيئةِ لا غيرُه، وإعادةُ تفعيلِه
موقوفةٌ على ضبطِ قواعدِ الحفظِ للمكتبةِ والتحقّقِ منها.

### أُضيف

- **تنزيلُ الفيديو** بجودةٍ يختارُها المستخدم: أفضلُ متاحٍ أو 1080p أو 720p أو 480p،
  مع دمجِ المسارَين في mp4.
- **استخراجُ الصوت** بستِّ صيغ: MP3 وM4A وOPUS وFLAC وWAV وVORBIS، وMP3 بأعلى جودة.
- **المشاركةُ إلى GMD**: يشاركُ المستخدمُ الرابطَ من أيِّ تطبيقٍ فيفتحُ GMD جاهزاً
  به ويقفزُ إلى شاشةِ الفيديو مباشرةً. وهو ما لا مقابلَ له في نسخةِ الحاسوب.
- **فتحُ الروابطِ مباشرةً** حين يختارُ المستخدمُ GMD لها.
- **معلوماتُ الوسائط** قبلَ التنزيل: العنوانُ والقناةُ والمدّةُ والدقّةُ والصيغةُ
  وعددُ المشاهدات.
- **خدمةٌ مقدِّمةٌ بإشعارِ تقدُّم**: التنزيلُ لا يتوقّفُ حين يغادرُ المستخدمُ التطبيق،
  فأندرويد يقتلُ العملَ الخلفيَّ بلا خدمةٍ مقدِّمة.
- **الحفظُ في معرضِ الوسائط** عبرَ MediaStore، بلا إذنِ كتابةٍ على أندرويد 10 فما فوق.
- **التحديثُ الذاتيّ**: يقرأُ إصداراتِ المستودع، وينزّلُ الحزمةَ، ويسلّمُها لمُثبِّتِ
  النظامِ عبرَ FileProvider.
- **تحديثُ yt-dlp** من داخلِ التطبيق.
- **تعريبٌ كاملٌ** مع الإنجليزيّة، وواجهةٌ تدعمُ الاتّجاهَين.

### أُضيف في alpha.1

- **معرضٌ داخلَ التطبيق.** كلُّ ما نزّله GMD في `Movies/GMD` و`Music/GMD` يُعرَضُ
  بصورةٍ مصغَّرةٍ ومدّةٍ وحجم: نقرةٌ تُشغّلُ المقطع، ولمسةٌ مطوّلةٌ تُحدِّده، وزرُّ
  تحديدِ الكلّ. والمحدَّدُ يُشارَكُ في ورقةٍ واحدةٍ أو يُحذَف. والحذفُ يُستأذَنُ فيه
  أوّلاً، فإن كان التطبيقُ قد فقدَ مِلكيّةَ الملفّاتِ — بعدَ إعادةِ تثبيتٍ مثلاً —
  انتقلَ إلى شاشةِ موافقةِ النظامِ على أندرويد 11 فما فوق بدلَ أن يفشلَ صامتاً.
- **المقاطعُ لا تضيعُ من المعرضِ بعدَ إعادةِ التثبيت.** ينسبُ أندرويد كلَّ ملفٍّ في
  MediaStore إلى الحزمةِ التي أنشأته فيُريها إيّاه بلا إذن، وهذه النسبةُ تبقى عندَ
  التحديثِ فوقَ الحزمةِ القائمةِ — وهو ما يفعلُه المحدِّثُ الذاتيُّ — وتسقطُ عندَ
  الإزالةِ ثمّ التثبيت. فتبقى المقاطعُ في `Movies/GMD` سليمةً ويَعمى عنها التطبيقُ،
  فيبدو المعرضُ فارغاً وقد ضاعَ كلُّ شيءٍ في ظنِّ المستخدم. صارَ إذنُ قراءةِ الوسائطِ
  مُعلَناً ويُطلَبُ **عندَ الحاجةِ إليه لا عندَ الإقلاع**، فالحالةُ الغالبةُ لا تحتاجُه:
  حينَ يكونُ المعرضُ فارغاً تشرحُ البطاقةُ السببَ وتعرضُ زرَّ «أظهِر ملفّاتي»، وحينَ
  يكونُ عامراً يبقى سطرٌ خافتٌ أسفلَه لمن ينقصُه مقاطعُ أقدم. ومَن رفضَ الإذنَ مرّتَين
  يمنعُه النظامُ من رؤيةِ النافذةِ ثانيةً فيعودُ الطلبُ بالرفضِ صامتاً — ويُستدَلُّ على
  ذلك بامتناعِ النظامِ عن عرضِ التبرير — فيتبدّلُ الزرُّ إلى «افتح إعداداتِ التطبيق»،
  ولا يُترَكُ المستخدمُ أمامَ زرٍّ لا يفعلُ شيئاً.
- **صورةُ المقطعِ وعنوانُه قبلَ التنزيل.** يُجلَبانِ من تلقائِهما بعدَ لصقِ الرابطِ
  بمهلةِ هدوءٍ قصيرة — بدونِها يُستدعى yt-dlp على كلِّ حرفٍ يُكتَب — وكلُّ استدعاءٍ
  يُلغي سابقَه فلا تصلُ إلّا نتيجةُ الرابطِ الأخير. يتيقّنُ المستخدمُ أنّ الرابطَ هو
  المقصودُ قبلَ تنزيلٍ قد يبلغُ مئاتِ الميغابايت.
- **اختيارُ لغةِ الواجهة** — تلقائيّ أو العربيّة أو الإنجليزيّة — كما في نسخةِ الحاسوب.
  حُفِظَ في SharedPreferences لا في DataStore عمداً: اللغةُ تُقرَأُ في
  `attachBaseContext` قبلَ إنشاءِ أيِّ مورد، وقراءةُ DataStore مُعلَّقةٌ واللحظةُ
  متزامنة. وطُبِّقَ على خدمةِ التنزيلِ أيضاً فتُكتَبُ الإشعاراتُ باللغةِ المختارة.

### الأمن

- **ترشيحُ المعرضِ قبلَ أندرويد 10 كان واسعاً.** `DATA LIKE '%/GMD/%'` يلتقطُ أيَّ
  مجلَّدٍ اسمُه GMD في الجهازِ لا مجلَّدَنا وحدَه، فصارَ مقيَّداً بـ`Movies/GMD`
  و`Music/GMD` كلٌّ بنوعِه.
- لا يُبنى نصُّ أمرٍ ولا يُمرَّرُ شيءٌ عبرَ صدفة: كلُّ قيمةٍ من المستخدم — الرابطُ
  قبلَ غيرِه — تدخلُ خانةً مستقلّةً في `YoutubeDLRequest`، فينتفي حقنُ الأوامرِ من
  أصلِه لا بالهروبِ منه. وهو الدرسُ نفسُه الذي فُرِضَ على نسخةِ الحاسوب في 26.9.0.
- الرابطُ يُقصُّ ممّا يحيطُه من فراغٍ وتُزالُ شَرطتُه البادئةُ كي لا يُقرأَ خياراً.

### التحزيم

- **حزمةٌ لكلِّ معماريّة** (‏arm64-v8a وarmeabi-v7a وx86 وx86_64) وحزمةٌ موحّدة:
  الأدواتُ تُشحَنُ ثنائيّاتٍ أصليّة، فالموحّدةُ تقاربُ 165 م.ب بينما حزمةُ المعماريّةِ
  الواحدةِ تقاربُ ثُلثَ ذلك.
- **رقمُ إصدارٍ مميّزٌ لكلِّ معماريّة**، وإلّا تعذّرَ الانتقالُ بينَ الحزم.
- **بياناتُ التبعيّاتِ الموقَّعةُ من غوغل مُزالة**، فوجودُها يمنعُ F-Droid من التحقّقِ
  من أنّ الحزمةَ بُنيَت من هذا المصدرِ بالضبط.

### أُصلح في alpha.1

- **زرُّ إعادةِ المحاولةِ في بطاقةِ الخطأ كان يحملُ نصَّ «تحقّق من وجود تحديث»** —
  مفتاحُ ترجمةٍ خاطئ. صارَ له نصُّه.
- **رسالةُ الفشلِ صارت صالحةً للإبلاغ**: اسمُ الصنفِ الكاملُ ورسالتُه وسلسلةُ
  الأسبابِ إلى أربعِ طبقات، مع زرِّ نسخٍ إلى الحافظة. كان يُعرَضُ اسمُ الصنفِ
  المختصرُ وحدَه، وهو في حزمةٍ مصغَّرةٍ مشوَّشٌ بلا معنى (‏`P2.f`).
- **التصغيرُ معطَّلٌ مؤقّتاً** في حزمِ الإصدار: هو أوّلُ المتّهمينَ بعطبِ تهيئةِ
  الأدواتِ لأنّه لا يظهرُ في حزمةِ debug، وتعطيلُه يفصلُ الفرضيّةَ بتجربةٍ واحدة.
  **وقد فصلَها**: التهيئةُ نجحت والتنزيلُ جرى، فالمتّهمُ مُدان.
- **الجودةُ صارت سقفاً على الضلعِ الأصغرِ لا على الارتفاع.** كان الترشيحُ
  `bv*[height<=720]+ba/best[height<=720]`، وهو صحيحٌ في الفيديو الأفقيِّ حيثُ
  الارتفاعُ هو الضلعُ الأصغر، وخاطئٌ في العموديِّ حيثُ ينقلبُ الأمر: مقطعُ «720p»
  عموديّاً مقاسُه 720×1280، فارتفاعُه 1280 يتجاوزُ كلَّ سقفٍ يطلبُه المستخدم —
  حتّى سقفَ 1080 — فلا تُطابِقُ الشرطَ صيغةٌ واحدةٌ ويموتُ التنزيلُ بـ
  `Requested format is not available`. ومقاطعُ فيسبوك عموديّةٌ في غالبِها فظهرَ
  العطبُ هناك أوّلاً، لا لأنّ للموقعِ خصوصيّة. صارَ الترشيحُ `-S res:N`، وحقلُ
  `res` عندَ yt-dlp هو الضلعُ الأصغرُ نفسُه فيَصحُّ في الاتّجاهَين، وهو ترتيبٌ لا
  شرطٌ قاطع: يُقدِّمُ الأقربَ عندَ السقفِ أو دونَه، فإن لم يكن إلّا ما فوقَه اختارَ
  أصغرَ ما فوقَه بدلَ أن يفشل. تُحقِّقَ منه على مقطعٍ عموديٍّ (720×1280 و1080×1920)
  وعلى مقطعٍ أفقيٍّ بأربعِ دقّات، والقديمُ يفشلُ في العموديِّ بأحدثِ yt-dlp أيضاً.
- **بطاقةُ الفشلِ صارت تدلُّ على الإجراء.** كان يُعرَضُ خرجُ yt-dlp خاماً، ويفتحُ
  بتحذيرِ قِدَمِ النسخةِ — خمسةِ أسطرٍ — فيغرقُ سطرُ `ERROR:` تحتَه. صارت أسطرُ
  الخطأِ تُرفَعُ إلى الأعلى، ويُترجَمُ العطبُ الشائعُ إلى نصيحةٍ عربيّةٍ (صيغةٌ غيرُ
  متاحة / تعذّرت قراءةُ الصفحة / تعذّرَ الاتّصال)، ويظهرُ معها زرُّ **تحديثِ yt-dlp**
  في موضعِ الحاجةِ إليه — فالمواقعُ تتغيّرُ أسرعَ من الحزمةِ المشحونةِ في التطبيق —
  وزرُّ نسخِ الخرجِ كاملاً للإبلاغ.

[26.9.0-alpha.1]: https://github.com/SalehGNUTUX/GMD-PHONE/releases/tag/v26.9.0-alpha.1
