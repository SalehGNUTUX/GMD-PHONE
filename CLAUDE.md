# CLAUDE.md

هذا الملفُّ يُرشِدُ Claude Code‏ (claude.ai/code) حينَ يعملُ على شيفرةِ هذا المستودع.

نسخةُ الهاتفِ من GMD: ‏Kotlin و Jetpack Compose و Material 3، تشحنُ yt-dlp وffmpeg
وaria2c وبايثون داخلَ الحزمةِ فتعملُ بلا خادمٍ وسيطٍ وبلا تثبيتِ شيء. ونسخةُ سطحِ
المكتبِ في مستودعٍ آخرَ (‏`SalehGNUTUX/GMD`) وتشاركُها سلسلةَ الترقيمِ نفسَها.

## الأوامر

```bash
# حزم الإصدار: خمس حزم — أربع معماريّات وواحدة موحّدة
./gradlew :app:assembleRelease

# حزمة التطوير (معرّفها com.gnutux.gmd.debug فتتعايش مع حزمة الإصدار)
./gradlew :app:assembleDebug

# تصريف سريع للتحقّق من الشيفرة وحدها
./gradlew :app:compileDebugKotlin
```

النواتجُ في `app/build/outputs/apk/<release|debug>/`. ولا اختباراتِ وحدةٍ في المشروع؛
الفحوصُ الآليّةُ في `.github/workflows/build.yml` وهي أربعةٌ تُشرَحُ أدناه.

**التوقيع:** يُقرَأُ من `keystore.properties` في الجذرِ (مستثنى من git) أو من متغيّراتِ
البيئةِ في CI. وبلا مفتاحٍ تُبنى الحزمةُ **غيرَ موقَّعةٍ** فيرفضُ أندرويد تثبيتَها —
يُترَكُ ذلك عمداً ليمرَّ البناءُ المحلّيُّ للتجربة، ويفشلُ النشرُ في CI صراحةً.

## مزالقُ تُقرَأُ قبلَ التعديل

كلُّ واحدٍ من هذه كلّفَ تشخيصاً، ولا أثرَ له في الشيفرةِ يدلُّ عليه.

### 1. الجودةُ سقفٌ على الضلعِ الأصغرِ لا على الارتفاع

لا تُرشِّح الصيغَ بـ`[height<=N]`. هذا صحيحٌ في الفيديو الأفقيِّ وحدَه؛ وفي العموديِّ
ينقلبُ الأمر: مقطعُ «720p» عموديّاً مقاسُه 720×1280، فارتفاعُه يتجاوزُ كلَّ سقفٍ
يطلبُه المستخدم — حتّى سقفَ 1080 — فلا تُطابِقُ الشرطَ صيغةٌ واحدةٌ ويموتُ التنزيلُ
بـ`Requested format is not available`. ومقاطعُ فيسبوك عموديّةٌ في غالبِها فظهرَ
العطبُ هناك أوّلاً، لا لأنّ للموقعِ خصوصيّة.

الصوابُ ما في `Downloader.Quality`: ‏`-f` ثابتٌ لكلِّ الجودات و`-S res:N` هو القيد،
لأنّ حقلَ `res` عندَ yt-dlp هو الضلعُ الأصغرُ نفسُه فيصحُّ في الاتّجاهَين. وهو ترتيبٌ
لا شرطٌ قاطع: يُقدِّمُ الأقربَ عندَ السقفِ أو دونَه، فإن لم يكن إلّا ما فوقَه اختارَ
أصغرَ ما فوقَه بدلَ أن يفشل. و`[height>0]` في `-f` يُبعِدُ الصيغَ مجهولةَ المقاسِ عن
التفضيلِ الأوّلِ لأنّ المجهولَ يتصدّرُ ترتيبَ `res` زوراً.

### 2. اللغةُ تُقرَأُ قبلَ وجودِ الموارد

`LocalePrefs` يستعملُ SharedPreferences لا DataStore التي يعتمدُها بقيّةُ الإعدادات،
وليسَ ذلك تناقضاً: اللغةُ تُقرَأُ في `attachBaseContext` — أي قبلَ إنشاءِ أيِّ مورد —
وقراءةُ DataStore مُعلَّقةٌ (‏suspend) واللحظةُ المطلوبةُ متزامنة. ولا سبيلَ إلى
تبديلِ لغةِ مواردَ محمَّلةٍ سلفاً في أندرويد، فتبديلُ اللغةِ يستدعي `recreate()`.
والتغليفُ مطبَّقٌ على **‏`MainActivity` وَ`DownloadService` معاً** وإلّا خرجت
الإشعاراتُ بلغةِ النظامِ بينما الواجهةُ بلغةٍ أخرى.

### 3. `ACTION_VIEW` بنوعٍ بلا عنوانٍ نيّةٌ ميّتة

`Intent(ACTION_VIEW).apply { type = "video/*" }` لا يقبلُها أيُّ تطبيقٍ في النظام،
ولا يحدثُ عندَ إطلاقِها شيءٌ ولا خطأ. كلُّ فتحٍ يمرُّ بعنوانِ `content://` الذي أعادَه
MediaStore مع `FLAG_GRANT_READ_URI_PERMISSION`. ولهذا يحملُ `Progress.Done` عنوانَ
الملفِّ لا اسمَه فقط.

### 4. مِلكيّةُ الملفّاتِ تسقطُ عندَ إعادةِ التثبيتِ لا عندَ التحديث

يُري أندرويد التطبيقَ ملفّاتِ MediaStore التي أنشأها بلا إذن. وهذه النسبةُ تبقى عندَ
التثبيتِ فوقَ الحزمةِ القائمةِ — وهو ما يفعلُه المحدِّثُ الذاتيُّ ما دامَ التوقيعُ
واحداً — وتسقطُ عندَ الإزالةِ ثمّ التثبيت. فتبقى المقاطعُ في `Movies/GMD` سليمةً
ويَعمى عنها التطبيقُ فيبدو المعرضُ فارغاً.

ولذلك يُعلَنُ إذنُ قراءةِ الوسائطِ ويُطلَبُ **عندَ الحاجةِ لا عندَ الإقلاع**. ومن رفضَه
مرّتَين يمنعُه النظامُ من رؤيةِ النافذةِ ثانيةً فيعودُ الطلبُ بالرفضِ صامتاً، ولا
يُميَّزُ ذلك إلّا بامتناعِ النظامِ عن عرضِ التبرير — فيتبدّلُ الزرُّ إلى فتحِ إعداداتِ
التطبيق. راجِع `GalleryScreen.requestAccess()`.

### 5. التصغيرُ يحتاجُ قواعدَ حفظٍ مكتوبةً بيدك

`isMinifyEnabled = true` منذ beta.1، لكنّ مكتبةَ `youtubedl-android` **لا تشحنُ
قواعدَ حفظٍ إطلاقاً** — لا `proguard.txt` في حزمتِها — فكلُّ ما يحميها هو ما في
`app/proguard-rules.pro`. وهي تقرأُ خرجَ الأدواتِ بـJackson بالانعكاس، فالتشويشُ
يُسقطُها وقتَ التشغيلِ لا وقتَ البناء. وهذا ما أسقطَ alpha.1.

ولا تُعدِّل تلك القواعدَ بلا تحقُّقٍ من داخلِ الحزمةِ نفسِها:

```bash
apkanalyzer dex packages --defined-only <apk> | awk '$1=="C"{print $NF}' | grep yausername
```

ينبغي أن تظهرَ الأصنافُ بأسمائِها الكاملةِ — ومنها `mapper.VideoInfo` و
`YoutubeDLException` — بينما تُشوَّشُ حزمُ الشيفرةِ الأخرى.

و`-keepnames class * extends java.lang.Throwable` قاعدةٌ لا تُحذَف: بلا جهازٍ متّصلٍ
بـlogcat فاسمُ الاستثناءِ كلُّ ما يصلُ من جهازِ المستخدم، وبدونِها يصلُ مشوَّشاً
(`P2.f`) فيضيعُ التشخيصُ كلُّه.

### 6. حارسُ الأرقامِ في CI كان يقرأُ الأيقونات

`grep -rP '[\x{0660}-\x{0669}]'` يقرأُ الملفَّ الثنائيَّ كما يقرأُ النصّ، وأيقوناتُ
`mipmap` تحوي في بايتاتِها ما يطابقُ نطاقَ الأرقامِ المشرقيّة — فكانَ يُفشِلُ كلَّ
جولةِ وسمٍ وليس في المشروعِ رقمٌ مشرقيٌّ واحد. يلزمُه `-I` ومرشّحاتُ `--include`.

## البنية

### الأدواتُ الأصليّة

`GmdApp` (‏`Application`) يستخرجُ بايثون وyt-dlp وffmpeg وaria2c من الحزمةِ عندَ أوّلِ
تشغيل، في `Dispatchers.IO`، ويكشفُ `tools: StateFlow<ToolsState>`. **لا تُستدعى عمليّةُ
تنزيلٍ قبلَ `ToolsState.Ready`** — والواجهةُ تُعطِّلُ أزرارَها حتّى ذلك. وعندَ الفشلِ
تُعرَضُ سلسلةُ الأسبابِ إلى أربعِ طبقاتٍ بأسماءِ الأصنافِ الكاملةِ مع زرِّ نسخ: بلا
جهازٍ متّصلٍ بـlogcat هذه هي الطريقُ الوحيدةُ ليصلَ الخطأُ من المستخدمِ إلى المطوِّر.

### الأمن: لا صدفةَ ولا حقنَ أوامر

كلُّ قيمةٍ من المستخدمِ — الرابطُ قبلَ غيرِه — تدخلُ خانةً مستقلّةً في
`YoutubeDLRequest.addOption`. لا يُبنى نصُّ أمرٍ ولا يُمرَّرُ شيءٌ عبرَ صدفةٍ البتّة،
وهو الدرسُ نفسُه الذي فُرِضَ على نسخةِ الحاسوبِ في 26.9.0. و`sanitize()` تقصُّ الفراغَ
وتُزيلُ الشَّرطةَ البادئةَ كي لا يُقرأَ الرابطُ خياراً.

### التنزيل

`DownloadService` خدمةٌ مُقدِّمةٌ بإشعارِ تقدُّم — ليست تحسيناً بل شرطَ عمل: أندرويد
يجمّدُ العمليّةَ حينَ تغادرُ الواجهةُ المقدّمة، فتنزيلُ مقطعٍ طويلٍ يموتُ في منتصفِه
لو جرى في نطاقِ شاشة. وقفلُ اليقظةِ يمنعُ نومَ المعالجِ معه. والحالةُ تُكشَفُ عبرَ
`DownloadService.progress: StateFlow<Progress>`.

النجاحُ يُقاسُ برمزِ الخروجِ **ووجودِ ملفٍّ فعليّ** — لا بالبحثِ عن كلمةِ `error` في
الخرج، فـyt-dlp يطبعُها على محاولةٍ فاشلةٍ ثمّ ينجحُ بالتالية.

### الحفظُ في معرضِ الوسائط

`MediaStoreSaver` ينقلُ الناتجَ من مجلَّدِ العملِ إلى `Movies/GMD` أو `Music/GMD` عبرَ
MediaStore. والمساراتُ الحرّةُ التي تعتمدُها نسخةُ الحاسوبِ لم تعد متاحةً منذ أندرويد
10. ويُستعمَلُ `IS_PENDING` أثناءَ النسخِ فلا يظهرُ في المعرضِ مقطعٌ نصفُ مكتوب،
ويُحذَفُ المدخلُ إن فشلَ النسخُ فلا يبقى مدخلٌ فارغ.

### المعرض

`MediaLibrary` يقرأُ المجلَّدَين ويتصرّفُ فيهما، و`GalleryScreen` يعرضُهما. الترشيحُ
بـ`RELATIVE_PATH` منذ أندرويد 10 وبالمسارِ الخامِّ قبلَه — **مقيَّداً بـ`Movies/GMD`
و`Music/GMD`**، فترشيحٌ بـ`%/GMD/%` وحدَه يلتقطُ أيَّ مجلَّدٍ بهذا الاسمِ في الجهاز.
والحذفُ يُستأذَنُ فيه أوّلاً، ثمّ يُجرَّبُ مباشرةً، فإن رمى النظامُ استثناءَ أمنٍ
انتقلَ إلى `MediaStore.createDeleteRequest` على أندرويد 11 فما فوق.

الشاشةُ تُغلَّفُ بعمودٍ مُمرَّرٍ في `GmdApp` (الواجهة)، فلا تُستعمَلُ فيها شبكةٌ كسولةٌ
لأنّ ارتفاعَها غيرُ محدودٍ داخلَ تمريرٍ آخرَ فينهارُ القياس.

### جلبُ المعلوماتِ تلقائيّاً

`GmdViewModel.setUrl` يُطلِقُ `fetchInfo` بعدَ مهلةِ هدوءٍ 700 مللي. المهلةُ ليست
زينة: الدالّةُ تُستدعى على كلِّ حرفٍ يُكتَب، وبلا تأخيرٍ يُشغَّلُ yt-dlp عشراتِ المرّاتِ
في ثانية. وكلُّ استدعاءٍ يُلغي سابقَه فلا تصلُ إلّا نتيجةُ الرابطِ الأخير. وإن فشلَ
الجلبُ لم تُعرَض في شاشةِ التنزيلِ رسالةٌ: التنزيلُ نفسُه سيُبلّغُ عن العطبِ إن وقع.

### التحديثُ الذاتيّ

`Updater` يقرأُ إصداراتِ المستودعِ من واجهةِ GitHub، ويختارُ الأصلَ المطابقَ لمعماريّةِ
الجهازِ بمطابقةِ `Build.SUPPORTED_ABIS` بأسماءِ الأصول — **ولهذا يجبُ أن يحملَ اسمُ كلِّ
حزمةٍ معماريّتَها**، وهو ما يضبطُه `outputFileName` في `build.gradle.kts`. ثمّ يُسلّمُ
الحزمةَ لمُثبِّتِ النظامِ عبرَ FileProvider، فأندرويد لا يقبلُ `file://` منذ السابعة.

### الترقيمُ والتحزيم

`versionName` في `build.gradle.kts` مصدرُه الوحيد، والسلسلةُ سنةٌ‑شهرٌ تُكتَبُ semver
بلا صفرٍ بادئ: سلسلةُ 26.09 رقمُها `26.9.0`. و`versionCode` لكلِّ حزمةٍ يُحسَبُ
`100 × الأساس + رقم المعماريّة`، لأنّ مُثبِّتَ أندرويد يرفضُ حزمةً برقمٍ أقلَّ من
المثبَّتة، فلو تشاركت المعماريّاتُ الرقمَ نفسَه لتعذّرَ الانتقالُ بينها.

## الترجمة

`res/values/strings.xml` (الإنجليزيّة) و`res/values-ar/strings.xml` (العربيّة)، و
`resourceConfigurations` يحصرُ الحزمةَ فيهما. **كلُّ نصٍّ يراه المستخدمُ له مدخلٌ في
الملفَّين معاً** — والآليّةُ تفحصُ تطابقَ المفاتيحِ وتُفشِلُ البناءَ إن اختلفا.

## أعرافٌ لازمة

- **الأرقامُ المغربيّةُ 0-9 حصراً**، لا المشرقيّةَ (الهنديّةَ، نطاقُ U+0660–U+0669)،
  في الواجهةِ والتوثيقِ والإيداعاتِ معاً. والآليّةُ تفحصُ ذلك.
- **الإيداعاتُ وسجلُّ التغييراتِ والتوثيقُ بالعربيّةِ الفصحى**، باسمِ SalehGNUTUX
  وحدَه بلا أيِّ مساهمٍ آخر.
- **كلُّ إصدارٍ تجريبيٌّ** (‏`--prerelease`) حتى يُؤذَنَ صراحةً بالمستقرّ.
- **الرخصةُ GPLv3** بملفِّ `LICENSE` كامل.

## الفحوصُ الآليّة

على وسمٍ يبدأُ بـ`v` أو يدويّاً من صفحةِ Actions. أربعةُ فحوصٍ تسبقُ البناء: تطابقُ
الوسمِ مع رقمِ الإصدار، وتطابقُ الترجمةِ بينَ اللغتَين، والأرقامُ المغربيّة، ثمّ
استعادةُ مفتاحِ التوقيع. والأسرارُ الأربعةُ المطلوبةُ مذكورةٌ في رأسِ ملفِّ سيرِ العمل.

## خارطةُ الطريق

في `ROADMAP.md`: ما بقيَ، وما يمنعُه، ومرتّباً بأثرِه على المستخدم.
